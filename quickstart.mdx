---
title: "Quickstart"
description: "Get your API key and make your first Flowra API request"
---

## Get started in three steps

Use the Flowra API with an API key. You create and manage keys in the dashboard.

### Step 1: Get an API key

<Steps>
  <Step title="Open the dashboard">
    Go to [Flowra Dashboard](https://flowra.dev) and sign in.
  </Step>
  <Step title="Create or select a project">
    Use an existing project or create one. API keys are scoped to a project.
  </Step>
  <Step title="Create an API key">
    In your project, open **Project → API Keys** (or **Settings → API Keys**). Create a new key and copy it. Store it securely; you won’t see the full key again.
  </Step>
</Steps>

<Warning>
  **Keep your API key secret.** Do not commit it to version control or expose it in client-side code.
</Warning>

### Step 2: Call the API

Authenticate by sending your API key in the **x-api-key** header or as **Authorization: Bearer &lt;api-key&gt;**.

**Example: get your profile**

```bash
curl -X GET "https://flowra.dev/api/v1/users/profile" \
  -H "x-api-key: YOUR_API_KEY"
```

**Example: list tools**

```bash
curl -X GET "https://flowra.dev/api/v1/tools?limit=10" \
  -H "x-api-key: YOUR_API_KEY"
```

Replace `YOUR_API_KEY` with your actual key.

### Step 3: Explore the API

- **[Authentication](/authentication)** — API key and headers in detail.
- **[API keys](/api-keys)** — Where to create keys and best practices.
- **[Tools catalog](/guides/tools-catalog)** — Search and browse 20,000+ tools by keyword, toolkit, and tags.
- **[API reference](/api-reference)** — Full endpoint list with request/response schemas.

<CardGroup cols={2}>
  <Card title="Authentication" icon="key" href="/authentication">
    Headers and security.
  </Card>
  <Card title="API reference" icon="terminal" href="/api-reference">
    All endpoints and schemas.
  </Card>
</CardGroup>
